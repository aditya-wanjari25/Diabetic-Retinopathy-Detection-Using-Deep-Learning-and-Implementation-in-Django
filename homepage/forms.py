from django.db.models import fields
from django.forms import ModelForm
from .models import *
# class ImageForm(ModelForm):
#     class Meta:
#         model = inputImage    #Name of the model to create a form for
#         fields = ['img']  #Here we mention what all fields from the student model we need, 
						
        
